# ########################################################
# XXX A lawless place for swashbuckling Type Pirates XXX #
##########################################################
#
# But pirates usually stick to a code, right?

# Employ a pirate gang as a merciless police force.

# @noinline Arr.size(::Type{<:AbstractArray}) =
#     error("(Pirate): attempt to get the axes/size information of an array *type*.\n" *
#           "Did you mean to use known_size instead?")
#
# @noinline Arr.axes(::Type{<:AbstractArray}) =
#     error("(Pirate): attempt to get the axes/size information of an array *type*.\n" *
#           "Did you mean to use known_size instead?")
